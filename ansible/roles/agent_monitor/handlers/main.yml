---
- name: Daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart agent-monitor
  ansible.builtin.systemd:
    name: agent-monitor
    state: restarted
  listen:
    - Daemon-reload   # pick up unit changes before restart
